# Set global options for all chunks
knitr::opts_chunk$set(
echo = TRUE,       # show code, set FALSE if you want only output
message = FALSE,   # hide package messages
warning = FALSE,   # hide warnings
cache = FALSE,
fig.width = 8,     # default figure width
fig.height = 6,    # default figure height
dpi = 150          # figure resolution
)
pheatmap(
mat_scaled,
cluster_rows = TRUE,
cluster_cols = TRUE,
show_rownames = TRUE,
show_colnames = FALSE,
fontsize_row = 8,
border_color = NA,
color = colorRampPalette(c("blue", "white", "red"))(100),
main = "Top 20 Significant miRNAs Across Compartments"
)
getwd()
getwd()
getwd()
setwd("/Users/Bioinfo_data/miRNA_compartments_analysis")
getwd()
setwd("/Users/Bioinfo_data/miRNA_compartments_analysis")
list.files("data/processed/"
list.files("data/processed")
list.files()
ist.files("data/processed/")
list.files("data/processed/")
setwd("/Users/Bioinfo_data/miRNA_compartments_analysis")
# Look at the first few rows of metadata
head(metadata)
getwd()
getwd()
setwd("~/Downloads")
# Look at the first few rows of metadata
head(metadata)
setwd("/Users/Bioinfo_data/miRNA_compartments_analysis")
# Look at the first few rows of metadata
head(metadata)
# Look at the first few rows of metadata
head(metadata)
library(DESeq2)
# Ye example data automatically aata hai
example_data <- system.file("extdata", "geneCountMatrix.txt", package = "DESeq2")
dim()
dim()
head()
# Pehle data load karo
example_file <- system.file("extdata", "geneCountMatrix.txt", package = "DESeq2")
counts <- read.table(example_file, header = TRUE, row.names = 1)
dim()
dims()
dim(counts)
head()
head(counts)
library(DESeq2)
counts <- read.table(example_fike, header = True, row.names = 1)
counts <- read.table(example_file, header = True, row.names = 1)
dim(counts)
head(counts)
library(DESeq2)
example_file <- system.file("extdata", "geneCountMatrix.txt", package = "DESeq2")
counts <- read.table(example_file, header = TRUE, row.names = 1)
getwd()
list.files()
# Set global options for all chunks
knitr::opts_chunk$set(
echo = TRUE,       # show code, set FALSE if you want only output
message = FALSE,   # hide package messages
warning = FALSE,   # hide warnings
cache = FALSE,
fig.width = 8,     # default figure width
fig.height = 6,    # default figure height
dpi = 150          # figure resolution
)
dds <- dds[rowSums(counts(dds)) > 5, => 3 ]
dds <- dds[rowSums(counts(dds)) > 0, ]
dds <- dds[rowSums(counts(dds)) > 0, ]
# Set global options for all chunks
knitr::opts_chunk$set(
echo = TRUE,       # show code, set FALSE if you want only output
message = FALSE,   # hide package messages
warning = FALSE,   # hide warnings
cache = FALSE,
fig.width = 8,     # default figure width
fig.height = 6,    # default figure height
dpi = 150          # figure resolution
)
library(DESeq2)
library(ggplot2)
library(pheatmap)
library(dplyr)
library(ggrepel)
library(DESeq2)
library(tidyr)
library(ggpubr)
library(tibble)
library(forcats)
library(VennDiagram)
counts <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/miRNA_count_matrix.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/metadata_mirna.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- metadata[, "Compartment", drop = FALSE]
counts[1:5, 1:5]
dim(counts)
head(metadata)
counts[] <- lapply(counts, function(x) as.numeric(as.character(x)))
counts_mat <- as.matrix(counts)
counts_mat[is.na(counts_mat)] <- 0
metadata <- metadata[colnames(counts_mat), , drop = FALSE]
dds <- DESeqDataSetFromMatrix(
countData = counts_mat,
colData = metadata,
design = ~ Compartment
)
library(DESeq2)
library(ggplot2)
library(pheatmap)
library(dplyr)
library(ggrepel)
library(DESeq2)
library(tidyr)
library(ggpubr)
library(tibble)
library(forcats)
library(VennDiagram)
counts <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/miRNA_count_matrix.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/metadata_mirna.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- metadata[, "Compartment", drop = FALSE]
counts[1:5, 1:5]
dim(counts)
head(metadata)
counts[] <- lapply(counts, function(x) as.numeric(as.character(x)))
counts_mat <- as.matrix(counts)
counts_mat[is.na(counts_mat)] <- 0
metadata <- metadata[colnames(counts_mat), , drop = FALSE]
dds <- DESeqDataSetFromMatrix(
countData = counts_mat,
colData = metadata,
design = ~ Compartment
)
dds <- dds[rowSums(counts(dds)) > 0, ]
sum(rowSums(counts(dds)) == 0)
keep <- rowSums(counts(dds) >= 5) >= 3
dds <- dds[keep, ]
dim(dds)
keep <- rowSums(counts(dds) >= 10) >= 3
dds <- dds[keep, ]
dim(dds)
keep <- rowSums(counts(dds) >= 1) >= 3
dds <- dds[keep, ]
dim(dds)
dim(dds)
keep <- rowSums(counts(dds) >= 10) >= 3
dds <- dds[keep, ]
dim(dds)
keep <- rowSums(counts(dds) >= 10) >= 1
dds <- dds[keep, ]
dim(dds)
dim(dds)
keep <- rowSums(counts(dds) >= 10) >= 1
dds <- dds[keep, ]
dim(dds)
keep <- rowSums(counts(dds) >=  10>= 1
keep <- rowSums(counts(dds) >=  10 >= 1
sum(rowSums(counts(dds) >= 10) >= 1)
dds <- dds[keep, ]
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
# Set global options for all chunks
knitr::opts_chunk$set(
echo = TRUE,       # show code, set FALSE if you want only output
message = FALSE,   # hide package messages
warning = FALSE,   # hide warnings
cache = FALSE,
fig.width = 8,     # default figure width
fig.height = 6,    # default figure height
dpi = 150          # figure resolution
)
library(DESeq2)
library(ggplot2)
library(pheatmap)
library(dplyr)
library(ggrepel)
library(DESeq2)
library(tidyr)
library(ggpubr)
library(tibble)
library(forcats)
library(VennDiagram)
counts <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/miRNA_count_matrix.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/metadata_mirna.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- metadata[, "Compartment", drop = FALSE]
counts[1:5, 1:5]
dim(counts)
head(metadata)
counts[] <- lapply(counts, function(x) as.numeric(as.character(x)))
counts_mat <- as.matrix(counts)
counts_mat[is.na(counts_mat)] <- 0
metadata <- metadata[colnames(counts_mat), , drop = FALSE]
dds <- DESeqDataSetFromMatrix(
countData = counts_mat,
colData = metadata,
design = ~ Compartment
)
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
sum(rowSums(counts(dds) >= 5) >= 1)
dds <- dds[keep, ]
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
dim(dds)
dim(dds)
dim(dds)
dim(dds)
dim(dds)
dim(dds)
dim(dds)
dim(dds)
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
dim(dds)
counts <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/miRNA_count_matrix.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/metadata_mirna.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- metadata[, "Compartment", drop = FALSE]
counts[1:5, 1:5]
dim(counts)
head(metadata)
counts[] <- lapply(counts, function(x) as.numeric(as.character(x)))
counts_mat <- as.matrix(counts)
counts_mat[is.na(counts_mat)] <- 0
metadata <- metadata[colnames(counts_mat), , drop = FALSE]
dds <- DESeqDataSetFromMatrix(
countData = counts_mat,
colData = metadata,
design = ~ Compartment
)
dim(dds)
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
dim(dds)
dds <- dds[keep, ]
dim(dds)
dim(dds)
dim(dds)
dim(dds)
dim(dds)
dim(dds)
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
dim(dds)
dds <- dds[keep, ]
exists("keep")
sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
dim(dds)
keep <- sum(rowSums(counts(dds) >= 5) >= 3)
dds <- dds[keep, ]
dim(dds)
keep <- rowSums(counts(dds) >= 5) >= 3
dds <- dds[keep, ]
dim(dds)
dim(dds)
dim(dds)
dim(dds)
dim(dds)
dim(dds)
dim(dds)
keep <- rowSums(counts(dds) >= 5) >= 3
keep <- rowSums(counts(dds) >= 5) >= 3
dds <- dds[keep, ]
dim(dds)
rowSums(counts(dds) >= 5) >= 3
rowSums(counts(dds) >= 5) >= 3
rowSums(counts(dds) >= 5) >= 3
rowSums(counts(dds) >= 5) >= 3
rowSums(counts(dds) >= 5) >= 3
dds <- dds[keep, ]
dim(dds)
counts <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/miRNA_count_matrix.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/metadata_mirna.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- metadata[, "Compartment", drop = FALSE]
counts[1:5, 1:5]
dim(counts)
head(metadata)
counts[] <- lapply(counts, function(x) as.numeric(as.character(x)))
counts_mat <- as.matrix(counts)
counts_mat[is.na(counts_mat)] <- 0
metadata <- metadata[colnames(counts_mat), , drop = FALSE]
dds <- DESeqDataSetFromMatrix(
countData = counts_mat,
colData = metadata,
design = ~ Compartment
)
dds <- dds[keep, ]
dds <- DESeqDataSetFromMatrix(
countData = counts_mat,
colData = metadata,
design = ~ Compartment
)
rowSums(counts(dds) >= 5) >= 3
dds <- dds[keep, ]
dim(dds)
dim(dds)
dim(dds)
rowSums(counts(dds) >= 5) >= 3
# Set global options for all chunks
knitr::opts_chunk$set(
echo = TRUE,       # show code, set FALSE if you want only output
message = FALSE,   # hide package messages
warning = FALSE,   # hide warnings
cache = FALSE,
fig.width = 8,     # default figure width
fig.height = 6,    # default figure height
dpi = 150          # figure resolution
)
library(DESeq2)
library(ggplot2)
library(pheatmap)
library(dplyr)
library(ggrepel)
library(DESeq2)
library(tidyr)
library(ggpubr)
library(tibble)
library(forcats)
library(VennDiagram)
counts <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/miRNA_count_matrix.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/metadata_mirna.csv",
row.names = 1,
head(metadata)
check.names = FALSE,
counts <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/miRNA_count_matrix.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/metadata_mirna.csv",
row.names = 1,
head(metadata)
check.names = FALSE,
counts <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/miRNA_count_matrix.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/metadata_mirna.csv",
row.names = 1,
head(metadata)
check.names = FALSE,
counts <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/miRNA_count_matrix.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/metadata_mirna.csv",
row.names = 1,
head(metadata)
check.names = FALSE,
# Set global options for all chunks
knitr::opts_chunk$set(
echo = TRUE,       # show code, set FALSE if you want only output
message = FALSE,   # hide package messages
warning = FALSE,   # hide warnings
cache = FALSE,
fig.width = 8,     # default figure width
fig.height = 6,    # default figure height
dpi = 150          # figure resolution
)
counts <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/miRNA_count_matrix.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/metadata_mirna.csv",
row.names = 1,
head(metadata)
check.names = FALSE,
library(DESeq2)
library(ggplot2)
library(pheatmap)
library(dplyr)
library(ggrepel)
library(DESeq2)
library(tidyr)
library(ggpubr)
library(tibble)
library(forcats)
library(VennDiagram)
counts <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/miRNA_count_matrix.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/metadata_mirna.csv",
row.names = 1,
head(metadata)
check.names = FALSE,
counts[1:5, 1:5]
dim(counts)
rowSums(counts(dds) >= 5) >= 3
rowSums(counts(dds) >= 5) >= 3
rowSums(counts(dds) >= 5) >= 3
rowSums(counts(dds) >= 5) >= 3
rowSums(counts(dds) >= 5) >= 3
rowSums(counts(dds) >= 5) >= 3
counts <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/miRNA_count_matrix.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/metadata_mirna.csv",
row.names = 1,
names = FALSE,
stringsAsFactors = FALSE)
counts <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/miRNA_count_matrix.csv",
row.names = 1,
check.names = FALSE,
stringsAsFactors = FALSE)
metadata <- read.csv("/Users/Bioinfo_data/miRNA_compartments_analysis/data/processed/metadata_mirna.csv",
row.names = 1,
names = FALSE,
stringsAsFactors = FALSE)
